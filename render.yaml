services:
  - type: web
    name: agrivision-ai
    runtime: python
    buildCommand: pip install -r agrivision/requirements-web.txt
    startCommand: uvicorn agrivision.app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
